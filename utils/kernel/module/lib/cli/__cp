## Copies file src to dest, either of them being local (relative to current location)
## or container-located in format (container_name:/path/to/dest).
##
## Usage: docktie_cli cp <src> <dest>
##
__cp() {
    local src=$1
    local dest=$2

    $DOCKER_COMPOSE_BIN cp $src $dest
} ## END: __cp()
